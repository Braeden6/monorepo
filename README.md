# Braeden6 Monorepo

This monorepo serves as the central repository for a personal website.

## Getting Started

To set up and run the project locally, use `pnpm`:

1.  **Install dependencies:**
    ```bash
    pnpm install
    ```
2.  **Start the development server:**
    ```bash
    pnpm dev
    ```
